const SectionTitle = ({ title }: { title: string }) => {
  return (
    <h2 className='text-2xl xs:text-3xl font-semibold font-outfit text-neutral-dark'>{title}</h2>
  )
}

export default SectionTitle